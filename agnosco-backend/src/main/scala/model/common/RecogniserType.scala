package model.common

/**
  * The differents format that the application can export to
  * Need to write the name of the class used
  */
object RecogniserType extends Enumeration {
	val Default, LaiaRecogniser = Value
}
